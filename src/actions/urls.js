import SERVER_URL from './misc/constants';

const urls = {
  LOGIN: `${SERVER_URL}/api/v1/account/login/`,
  FORGOTPASSWORD: `${SERVER_URL}/api/v1/accounts/forgot-password/`,
  LOGOUT: `${SERVER_URL}/api/v1/accounts/forgot-password/`
};

export default urls;
